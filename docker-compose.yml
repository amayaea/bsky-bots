services:
  is-alive:
    build:
      dockerfile: bots/is-alive/Dockerfile
  sotd:
    build:
      dockerfile: bots/sotd/Dockerfile
  fantasy-bball:
    build:
      dockerfile: bots/fantasy-bball/Dockerfile
